# project-b

A tiny React-style frontend placeholder used for SkillHost integration testing.

The repository is intentionally lightweight but realistic enough for testing:

- `src/` contains a minimal UI component and app entry.
- `package.json` contains common frontend scripts.
- `.agents/skills/` contains project-level frontend skills.

## Commands

```bash
npm install
npm run dev
npm run build
```
